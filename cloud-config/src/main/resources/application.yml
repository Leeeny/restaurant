server:
  port: 8090

spring:
  application:
    name: cloud-config
  config:
    import:
      - optional:file:.env[.properties]
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Leeeny/stepicMicroserviceConfig.git
          default-label: master
          username: Leeeny
          password: ${GITHUB_TOKEN}