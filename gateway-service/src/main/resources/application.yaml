spring:
  config:
    import:
      - optional:file:.env[.properties]
      - configserver:http://${CONFIG_SERVER_URL:localhost}:8090
  application:
    name: gateway-service

management:
  endpoints:
    web:
      exposure:
        include: "*"